<%= turbo_stream.update "results" do %>
  <div class="bg-white rounded-lg shadow-lg p-6 max-w-md w-full">
    <h3 class="text-xl font-bold text-gray-800 mb-4 text-center">💰 Salary Breakdown</h3>
    <div class="space-y-3">
      <div class="flex justify-between items-center p-3 bg-green-50 rounded-lg border-l-4 border-green-400">
        <span class="font-medium text-gray-700">Hourly:</span>
        <span class="text-lg font-bold text-green-600">€<%= number_with_precision(@hourly, precision: 2) %></span>
      </div>
      <div class="flex justify-between items-center p-3 bg-blue-50 rounded-lg border-l-4 border-blue-400">
        <span class="font-medium text-gray-700">Daily (8h):</span>
        <span class="text-lg font-bold text-blue-600">€<%= number_with_precision(@daily, precision: 2) %></span>
      </div>
      <div class="flex justify-between items-center p-3 bg-purple-50 rounded-lg border-l-4 border-purple-400">
        <span class="font-medium text-gray-700">Monthly:</span>
        <span class="text-lg font-bold text-purple-600">€<%= number_with_precision(@monthly, precision: 2) %></span>
      </div>
      <div class="flex justify-between items-center p-3 bg-indigo-50 rounded-lg border-l-4 border-indigo-400">
        <span class="font-medium text-gray-700">Annual:</span>
        <span class="text-xl font-bold text-indigo-600">€<%= number_with_precision(@annual, precision: 2) %></span>
      </div>
    </div>
  </div>
<% end %>
